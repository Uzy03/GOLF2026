# CLAUDE.md - Team Development Protocol

## 🚨 CRITICAL SAFETY ALERT
- NO AXIOS: axiosライブラリはマルウェア汚染の報告があるため、絶対に使用しないこと。
- ALTERNATIVE: HTTP通信には、Node.js 18+ のネイティブ fetch API、または undici / node:http を使用すること。

## 🤝 Team Workflow

### 役割定義
- **Claude（上司）**: 要件整理・設計・タスク分解・コードレビュー・最終確認
- **Copilot（部下）**: コードファイルの実装・バグ修正・テスト作成

### 委任ルール — 以下は必ず Copilot に委任すること
- .cs / .ts / .tsx / .js / .jsx / .py ファイルの新規作成（10行以上）
- 既存コードへの大幅な変更・リファクタリング
- テストコードの作成

### 自分で行うこと（委任しない）
- 設定ファイル（.json / .yaml / .md / .toml / .sh）の変更
- 小さな修正（1〜5行程度）
- コードレビューとフィードバック
- タスク設計・仕様書作成

### 🤖 作業員モデル選択ルール（委任前に必ず判断すること）

| エイリアス | モデル ID | コスト | 用途 |
|---|---|---|---|
| `A` **(デフォルト)** | `claude-haiku-4.5` | 0.33x | 標準的な実装全般。迷ったらこれ |
| `B` | `gpt-5-mini` | **0x** | コードを書かない単純作業 |
| `C` | `claude-sonnet-4.5` | 1x | 複雑な実装・深いバグ修正 |
| `D` | `claude-opus-4.5` | 3x | 最難関タスク（滅多に使わない） |

**タスク別モデル選択ガイド:**

```
■ A (haiku) を使う場合 — デフォルト
  - あらゆる言語の定型コード生成（TypeScript / Python / C# / Go 等）
  - ユニットテスト・モックの作成
  - 既存コードへの機能追加（ロジックが明確なもの）
  - エラーログの解析・バグ修正（原因が特定済みのもの）
  - API クライアント・データクラス・型定義の実装

■ B (gpt-5-mini) を使う場合 — 無料枠・思考不要
  - ファイルのリネーム・移動・整理
  - README / コメント / ドキュメントの草案
  - JSON / YAML / CSV の整形・変換
  - import の整理・lint エラーの一括修正
  - 単純なリファクタリング（ロジック変更なし）

■ C (sonnet) を使う場合 — 高知能・コスト高
  - 複雑なアルゴリズム実装（GNN・物理シミュ・最適化など）
  - 根本原因が不明なバグの調査と修正
  - 複数ファイルにまたがるアーキテクチャ変更
  - A に委任して失敗・品質不足だったタスクの再委任
  - 新しい技術スタックの初期実装

■ D (opus) を使う場合 — 最終手段
  - C でも解決できなかった最難関タスク
  - 重大なシステム設計の判断を要するもの
```

**判断フロー:** まず `A` で委任 → 品質不足なら `C` で再委任 → それでも駄目なら `D`。`B` は「コードを書かない」タスク専用。

### 委任手順（必ずこの順序で）
1. 実装仕様を具体的に考える（ファイル名・クラス名・メソッド・ロジック）
2. モデルを選択し、以下を実行して Copilot に委任する:
   ```bash
   bash team/delegate.sh "詳細な実装仕様をここに書く"          # デフォルト(A=haiku)
   bash team/delegate.sh --model B "..."                        # 単純作業
   bash team/delegate.sh --model C "複雑な実装仕様"             # 難しいタスク
   bash team/delegate.sh --model D "..."                        # 最終手段
   ```
3. Copilot がバックグラウンドで作業し、完了まで自動待機する
4. `team/report.md` の内容が返ってきたらレビューする
5. 修正が必要な場合はフィードバックを書いて再委任する

### 委任時の仕様書の書き方（必ず含める）
- 作成/修正するファイルパス（絶対パスで記載）
- クラス名・関数名・メソッド名
- 依存関係（他ファイルとのつながり）
- 期待する動作

## Workflow Orchestration

### 1. Plan First
- 3ステップ以上のタスク、またはアーキテクチャ上の決定が必要な場合は、必ず Plan Mode に入る。
- 問題が発生した場合は即座に停止し、再計画（RE-PLAN）する。
- 構築だけでなく検証時も Plan Mode を使用し、曖昧さを排除した詳細スペックを事前に記述する。

### 2. Subagent Strategy
- メインコンテキストを清潔に保つため、サブエージェントを積極的に使用する。
- 調査、並列分析、複雑な探索はサブエージェントにオフロードする。
- 1つのサブエージェントにつき、1つのタスクに集中させる。

### 3. Self-Improvement Loop
- ユーザーからの修正後は、必ず `tasks/lessons.md` を更新し、再発防止のパターンを記録する。
- 誤り率が低下するまで、学んだ教訓を執念深く反復適用する。
- セッション開始時に、関連するプロジェクトの教訓を見直す。

### 4. Verification Before Done
- 動作の証明（Proof it works）なしにタスクを完了と見なさない。
- 変更による挙動の差異（Diff）を確認する。
- 常に「スタッフエンジニアがこれを承認するか？」と自問自答する。
- テストの実行、ログのチェック、正当性の実証を徹底する。

### 5. Demand Elegance (Balanced)
- 常に「よりエレガントな解決策」がないか模索する。
- 場当たり的な修正（Hacky fix）を避け、現在の知識で最高の実装を行う。
- 単純な修正には過剰な設計をせず、迅速に対応する。
- 提出前に、自身の成果物に挑戦（Challenge）する。

### 6. Autonomous Bug Fixing
- バグ報告には自律的に対処する。手取り足取りの説明を求めない。
- ログやエラー、失敗したテストから根本原因を特定し、解決する。
- ユーザーにコンテキストの切り替えを要求しない。

## Task Management
- Plan First: `tasks/todo.md` にチェックリスト形式の計画を記述。
- Verify Plan: 実装前に計画の妥当性を確認。
- Track Progress: 進行状況に応じて完了項目をマーク。
- Explain Changes: 各ステップで何を行ったかハイレベルな概要を説明。
- Document Results: `tasks/todo.md` にレビューセクションを追加。
- Capture Lessons: 修正後に `tasks/lessons.md` を更新。

### tasks/lessons.md の書き方
ユーザーからの修正・指摘を受けた後、以下フォーマットで追記する:

```markdown
## [YYYY-MM-DD] [カテゴリ] タイトル

- **問題**: 何が起きたか（症状）
- **原因**: なぜ起きたか（根本原因）
- **対策**: 次回どうするか（再発防止パターン）
```

カテゴリ例: `設計` / `実装` / `委任` / `テスト` / `コミュニケーション`

## Core Principles
- Simplicity First: すべての変更を可能な限りシンプルにする。コードへの影響を最小限に抑える。
- No Laziness: 根本原因（Root Cause）を追求し、一時的な対応を許さない。シニア開発者の基準を維持する。
- Minimal Impact: 変更は必要な箇所のみに留める。新たなバグの混入を避ける。
